@page "/nop-cs"
@using MyInterLibrary
<h1>Primes C#</h1>

<PerfomanceTester Action="@Action" />

@functions
{
    double Action(int n)
    {
        for (int i = 0; i < n; i++)
        {
            for (int j = 0; j < 1000; j++)
            {
                InterTest.Nop(n);
            }
        }

        return 0;
    }
}
